[gd_scene format=3 uid="uid://cricdh2ww2f11"]

[ext_resource type="Texture2D" uid="uid://cvhsmx3hlrhr6" path="res://Assets/special effects/WhiteCircle_1_1.png" id="1_q8u5g"]
[ext_resource type="PackedScene" uid="uid://jg6xu7axrwp" path="res://scenes/black_hole/black_hole.tscn" id="2_0bbpv"]
[ext_resource type="PackedScene" uid="uid://phayocmpwem2" path="res://scenes/player/player.tscn" id="4_rarhs"]
[ext_resource type="PackedScene" uid="uid://celrucoq6w7rx" path="res://scenes/notes/notes.tscn" id="4_vcsgt"]
[ext_resource type="Script" uid="uid://b0pew4hrhr1a0" path="res://scenes/ui and visuals/camera_2d.gd" id="5_vcsgt"]
[ext_resource type="Script" uid="uid://c7hol7juep6re" path="res://scenes/ui and visuals/ui.gd" id="6_nxtc6"]
[ext_resource type="FontFile" uid="uid://4wh2nbypg2" path="res://Assets/font/IBMPlexSans-VariableFont_wdth,wght.ttf" id="7_nxtc6"]
[ext_resource type="Script" uid="uid://de5j23ysicdd5" path="res://scenes/notes/Chart1.gd" id="8_jlsqs"]
[ext_resource type="Shader" uid="uid://732i5j48dd6p" path="res://scenes/ui and visuals/MainBG.gdshader" id="8_qmy6f"]
[ext_resource type="Texture2D" uid="uid://dkj5dlljvquup" path="res://Assets/ui/gr_optionarrow.png" id="9_e2s0e"]
[ext_resource type="Texture2D" uid="uid://chr30pd3ic7ch" path="res://Assets/ui/gr_optionarrow_hover.png" id="10_gsxep"]
[ext_resource type="PackedScene" uid="uid://d0vlgagekl3sa" path="res://scenes/levels/level_base.tscn" id="12_fs0b5"]
[ext_resource type="Texture2D" uid="uid://ndlgkxwxg54a" path="res://Assets/Sprites/covers/Cover1.png" id="13_hq6as"]
[ext_resource type="Resource" uid="uid://et45168hj3r3" path="res://resources/songs/Oh My!.tres" id="13_ma7h6"]
[ext_resource type="Resource" uid="uid://rcxiw3bhmpqn" path="res://resources/songs/Cytoplasm.tres" id="14_gsxep"]
[ext_resource type="Texture2D" uid="uid://d075ssde2e8dg" path="res://Assets/Sprites/covers/Cover1_hover.png" id="14_kxyth"]
[ext_resource type="Resource" uid="uid://b6eddy0eewpxb" path="res://resources/songs/Fallen Empire.tres" id="15_v0vrg"]
[ext_resource type="Texture2D" uid="uid://cw4qnxaldeoax" path="res://Assets/Sprites/covers/Cover2.png" id="16_cobve"]
[ext_resource type="Texture2D" uid="uid://bjbcui3pcrh17" path="res://Assets/Sprites/cursor1.png" id="17_1iba3"]
[ext_resource type="Texture2D" uid="uid://tcq8hy7eweao" path="res://Assets/Sprites/covers/Cover2_hover.png" id="17_cmqll"]
[ext_resource type="Texture2D" uid="uid://ka0u6drgxh8o" path="res://Assets/Sprites/cursor2.png" id="18_yxlcp"]
[ext_resource type="Texture2D" uid="uid://bpbiyk4s4eert" path="res://Assets/Sprites/covers/Cover3.png" id="19_8cp6k"]
[ext_resource type="Texture2D" uid="uid://ceuownhdrok5m" path="res://Assets/Sprites/cursor3.png" id="19_ebg2g"]
[ext_resource type="Texture2D" uid="uid://bh2lovuswy0c1" path="res://Assets/Sprites/cursor4.png" id="20_2ofn6"]
[ext_resource type="Texture2D" uid="uid://cu2o83eonxn4c" path="res://Assets/Sprites/covers/Cover3_hover.png" id="20_8b871"]
[ext_resource type="Texture2D" uid="uid://duhi71uc3f8jn" path="res://Assets/Sprites/covers/Cover4.png" id="21_8b871"]
[ext_resource type="Resource" uid="uid://bj4l508i6ovjs" path="res://resources/songs/Stellar ballad.tres" id="21_cmqll"]
[ext_resource type="Script" uid="uid://blpkpokykn85o" path="res://scenes/player/animated_sprite_2d.gd" id="21_xa025"]
[ext_resource type="Resource" uid="uid://brlpjmmoxfmni" path="res://resources/songs/Sticky eyes.tres" id="22_8cp6k"]
[ext_resource type="Texture2D" uid="uid://cix1nswnlr7lw" path="res://Assets/Sprites/covers/Cover4_hover.png" id="22_pdwbg"]
[ext_resource type="PackedScene" uid="uid://doaph78ntmae6" path="res://scenes/vfx/dust_fogs.tscn" id="22_yxlcp"]
[ext_resource type="Resource" uid="uid://5yaur3b2grrd" path="res://resources/songs/SpacetimeRift.tres" id="23_8b871"]
[ext_resource type="Texture2D" uid="uid://gkjfli0vsq4o" path="res://Assets/Sprites/covers/Cover7.png" id="24_5grtr"]
[ext_resource type="Resource" uid="uid://dfitmdopxvif2" path="res://resources/songs/Last Singularity.tres" id="24_pdwbg"]
[ext_resource type="PackedScene" uid="uid://bx6nbxye3bsx3" path="res://scenes/ui and visuals/main_menu.tscn" id="25_pnhog"]
[ext_resource type="Texture2D" uid="uid://r0le3bhu62n2" path="res://Assets/Sprites/covers/Cover7_hover.png" id="25_vua0o"]
[ext_resource type="Texture2D" uid="uid://cepneff06swfl" path="res://Assets/Sprites/covers/Cover6.png" id="27_t70y2"]
[ext_resource type="Texture2D" uid="uid://dp2iqa0fvtrdi" path="res://Assets/Sprites/covers/Cover6_hover.png" id="28_8gny2"]
[ext_resource type="Texture2D" uid="uid://d1yh473tbni7g" path="res://Assets/Sprites/covers/Cover8.png" id="30_s3wmy"]
[ext_resource type="Texture2D" uid="uid://bd0dk3uvwmg4f" path="res://Assets/Sprites/covers/Cover8_hover.png" id="31_gdcyb"]
[ext_resource type="PackedScene" uid="uid://dgwh12sjbpwmg" path="res://scenes/save_system/save_stuff.tscn" id="42_pdwbg"]

[sub_resource type="Gradient" id="Gradient_cvmbd"]
offsets = PackedFloat32Array(0, 0.18627451, 0.4869281, 0.8006536, 1)
colors = PackedColorArray(0.96862745, 0.972549, 0.972549, 0, 1, 1, 1, 1, 1, 1, 1, 0.5176471, 1, 1, 1, 1, 1, 1, 1, 0)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_1rfy5"]
gradient = SubResource("Gradient_cvmbd")

[sub_resource type="ParticleProcessMaterial" id="ParticleProcessMaterial_e5dxq"]
particle_flag_disable_z = true
emission_shape_scale = Vector3(2000, 1000, 1)
emission_shape = 3
emission_box_extents = Vector3(1, 1, 1)
initial_velocity_min = -4.5
initial_velocity_max = 4.5
gravity = Vector3(0, 0, 0)
scale_min = 0.19999999
scale_max = 0.29999998
color_ramp = SubResource("GradientTexture1D_1rfy5")
turbulence_noise_scale = 0.0

[sub_resource type="Gradient" id="Gradient_qmy6f"]
offsets = PackedFloat32Array(0, 0.7132353)
colors = PackedColorArray(0, 0.6784314, 0.9882353, 1, 0.16470589, 1, 0.38039216, 1)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_qmy6f"]
gradient = SubResource("Gradient_qmy6f")

[sub_resource type="ShaderMaterial" id="ShaderMaterial_bjd11"]
shader = ExtResource("8_qmy6f")
shader_parameter/pixel_scale = 466.0
shader_parameter/surface_radius = 0.34100001549897996
shader_parameter/surface_palette = SubResource("GradientTexture1D_qmy6f")
shader_parameter/background_colour = Color(0, 0, 0, 1)
shader_parameter/surface_base_rotation = 0.0
shader_parameter/surface_rotation_speed = 0.3330000633174999
shader_parameter/surface_primary_cell_amount = 10
shader_parameter/surface_primary_strength = 1.5199998542649595
shader_parameter/surface_primary_period = Vector3(9.5, 9.5, 9.5)
shader_parameter/surface_secondary_enabled = true
shader_parameter/surface_secondary_cell_amount = 20
shader_parameter/surface_secondary_strength = 0.3
shader_parameter/surface_secondary_period = Vector3(9.5, 9.5, 9.5)
shader_parameter/surface_secondary_rotation = 0.0
shader_parameter/surface_secondary_rotation_speed = 0.0
shader_parameter/surface_tertiary_enabled = false
shader_parameter/surface_tertiary_cell_amount = 30
shader_parameter/surface_tertiary_strength = 0.3
shader_parameter/surface_tertiary_period = Vector3(9.5, 9.5, 9.5)
shader_parameter/surface_tertiary_rotation = 0.0
shader_parameter/surface_tertiary_rotation_speed = 0.0
shader_parameter/surface_bump_strength = 0.3
shader_parameter/display_cloud_shadows = true
shader_parameter/cloud_shadow_darkness = 0.25
shader_parameter/cloud_1_cover = 0.3
shader_parameter/cloud_1_radius = 0.32
shader_parameter/cloud_1_cell_amount = 10
shader_parameter/cloud_1_rotation_speed = 0.03
shader_parameter/cloud_1_fluctuation_speed = 0.03
shader_parameter/cloud_1_period = Vector3(9.5, 9.5, 9.5)
shader_parameter/cloud_1_secondary_period = Vector3(9.5, 9.5, 9.5)
shader_parameter/cloud_1_secondary_cell_amount = 10
shader_parameter/cloud_1_secondary_strength = 0.3
shader_parameter/cloud_2_cover = 0.0
shader_parameter/cloud_2_radius = 0.32
shader_parameter/cloud_2_cell_amount = 10
shader_parameter/cloud_2_rotation_speed = 0.03
shader_parameter/cloud_2_fluctuation_speed = 0.03
shader_parameter/cloud_2_period = Vector3(9.5, 9.5, 9.5)
shader_parameter/cloud_2_secondary_period = Vector3(9.5, 9.5, 9.5)
shader_parameter/cloud_2_secondary_cell_amount = 10
shader_parameter/cloud_2_secondary_strength = 0.3
shader_parameter/atmosphere_radius = 0.34
shader_parameter/posterise = false
shader_parameter/shading_mode = 2
shader_parameter/maximum_brightness = 1.0
shader_parameter/sun_strength = 2.0
shader_parameter/shade_transition = 10.0
shader_parameter/shade_step = 8.0
shader_parameter/sun_direction = Vector3(1, 0.2, -0.3)
shader_parameter/dither_levels = 0
shader_parameter/shading_noise_cell_amount = 40
shader_parameter/shading_noise_strength = 0.3
shader_parameter/shading_noise_period = Vector3(9.5, 9.5, 9.5)

[sub_resource type="Shader" id="Shader_qmy6f"]
code = "shader_type canvas_item;
render_mode unshaded;

const int SHADING_MODE_NONE = 0;
const int SHADING_MODE_SMOOTH = 1;
const int SHADING_MODE_QUANT = 2;
const int SHADING_MODE_DITHER = 3;
const int SHADING_MODE_MAX = 4;

const float COLOUR_ESTIMATION_CUTOFF = 0.001;

// This should usually be set to the side of the rect that is displaying the
// planet, to match the size of pixels of sprites.
uniform float pixel_scale = 360.0;


//// Surface
uniform float surface_radius : hint_range(0.01, 0.5, 0.001) = .3;
// Palette for the surface.
// The colours should be in a single row ordered from lowest to highest.
// If a second row is present, cutoff values for the colours above will be
// taken from the red channel, with animation values taken from the green
// channel. See the 'surface' function for details of how these are used.
uniform sampler2D surface_palette : filter_nearest, hint_default_white;
uniform vec4 background_colour : source_color = vec4(0., 0., 0., 1.0);
// Base rotation of the planet on its axis.
uniform float surface_base_rotation : hint_range(0.0, 6.28, 0.1) = 0.0;
// Speed at which to animate the rotation of the planet.
uniform float surface_rotation_speed : hint_range(-1.0, 1.0, 0.001) = 0.0;

// Primary noise
uniform int surface_primary_cell_amount = 10;
// Factor by which to multiply the generated noise.
uniform float surface_primary_strength : hint_range(-5.0, 5.0, 0.01) = 1.0;
uniform vec3 surface_primary_period = vec3(9.5, 9.5, 9.5);

// Secondary noise
uniform bool surface_secondary_enabled = true;
uniform int surface_secondary_cell_amount = 20;
// Factor by which to multiply the generated noise.
uniform float surface_secondary_strength : hint_range(-5.0, 5.0, 0.01) = 0.3;
uniform vec3 surface_secondary_period = vec3(9.5, 9.5, 9.5);
// Amount to rotate this noise source relative to the primary.
uniform float surface_secondary_rotation : hint_range(0.0, 6.28, 0.1) = 0.0;
// Speed at which to animate the rotation of this source relative to the primary.
// This can be used to generate apparent motion of the whole surface, as if fluid.
uniform float surface_secondary_rotation_speed : hint_range(-1.0, 1.0, 0.001) = 0.0;

// Tertiary noise
uniform bool surface_tertiary_enabled = false;
uniform int surface_tertiary_cell_amount = 30;
// Factor by which to multiply the generated noise.
uniform float surface_tertiary_strength : hint_range(-5.0, 5.0, 0.01) = 0.3;
uniform vec3 surface_tertiary_period = vec3(9.5, 9.5, 9.5);
// Amount to rotate this noise source relative to the primary.
uniform float surface_tertiary_rotation : hint_range(0.0, 6.28, 0.1) = 0.0;
// Speed at which to animate the rotation of this source relative to the primary.
// This can be used to generate apparent motion of the whole surface, as if fluid.
uniform float surface_tertiary_rotation_speed : hint_range(-1.0, 1.0, 0.001) = 0.0;

// Non-noise bumpmap.
// This is added to the primary noise, before the secondary and tertiary noise
// is added.
uniform sampler2D surface_bump_map : hint_default_transparent, repeat_enable;
// Factor by which to multiply the bump map values.
uniform float surface_bump_strength : hint_range(-1.0, 1.0, 0.01) = 0.3;


//// Clouds
uniform bool display_cloud_shadows = true;
uniform float cloud_shadow_darkness : hint_range(0.0, 1.0, 0.05) = 0.25;

// First cloud layer
uniform float cloud_1_cover : hint_range(0.0, 1.0, 0.01) = 0.3;
uniform float cloud_1_radius : hint_range(0.01, 0.5, 0.001) = .32;
// Palette for the clouds.
// Works the same as the surface palette.
uniform sampler2D cloud_1_palette: filter_nearest, hint_default_transparent;
uniform int cloud_1_cell_amount = 10;
// The speed at which the clouds move over the surface.
uniform float cloud_1_rotation_speed : hint_range(-1.0, 1.0, 0.001) = 0.03;
// The speed at which the clouds change shape.
uniform float cloud_1_fluctuation_speed : hint_range(-1.0, 1.0, 0.001) = 0.03;
uniform vec3 cloud_1_period = vec3(9.5, 9.5, 9.5);
uniform vec3 cloud_1_secondary_period = vec3(9.5, 9.5, 9.5);
uniform int cloud_1_secondary_cell_amount = 10;
// Factor by which to multiply the generated noise.
uniform float cloud_1_secondary_strength : hint_range(-1.0, 1.0, 0.01) = 0.3;

// Second cloud layer
uniform float cloud_2_cover : hint_range(0.0, 1.0, 0.01) = 0.0;
uniform float cloud_2_radius : hint_range(0.01, 0.5, 0.001) = .32;
// Palette for the clouds.
// Works the same as the surface palette.
uniform sampler2D cloud_2_palette: filter_nearest, hint_default_transparent;
uniform int cloud_2_cell_amount = 10;
// The speed at which the clouds move over the surface.
uniform float cloud_2_rotation_speed : hint_range(-1.0, 1.0, 0.001) = 0.03;
// The speed at which the clouds change shape.
uniform float cloud_2_fluctuation_speed : hint_range(-1.0, 1.0, 0.001) = 0.03;
uniform vec3 cloud_2_period = vec3(9.5, 9.5, 9.5);
uniform vec3 cloud_2_secondary_period = vec3(9.5, 9.5, 9.5);
uniform int cloud_2_secondary_cell_amount = 10;
// Factor by which to multiply the generated noise.
uniform float cloud_2_secondary_strength : hint_range(-1.0, 1.0, 0.01) = 0.3;


//// Atmosphere
uniform float atmosphere_radius : hint_range(0.01, 0.5, 0.001) = .34;
// Palette for the atmosphere.
// The colours should be in a single row ordered from lowest to highest.
// Additional rows will be ignored.
uniform sampler2D atmosphere_palette : filter_nearest, hint_default_transparent;


//// Posterisation
uniform bool posterise = false;
// Palette to restrict the calculated colours to. All colours in the palette
// are considered.
uniform sampler2D posterisation_palette : filter_nearest, hint_default_black;


//// Light/shading
uniform int shading_mode : hint_enum(
	\"None\",
	\"Smooth\",
	\"Quantised\",
	\"Dithered\",
	\"Max Brightness\"
) = 2;
// A cap on the brightness produces by the light.
uniform float maximum_brightness : hint_range(0.0, 10.0, 0.1) = 1.0;
uniform float sun_strength = 2.0;
// Controls the width of the transition from light to dark. Higher values result
// in a quicker transition. For Quantised shading, this means narrower bands of
// different shades.
uniform float shade_transition = 10.0;
// Also contributes to the width of the transition from light to dark.
// For Quantised shading, higher values result in more bands.
uniform float shade_step = 8.0;
// Vector pointing to the sun. This must have some magnitude or the lighting
// will not work.
uniform vec3 sun_direction = vec3(1.0, 0.2, -0.3);
// Textures for different shades when using the \"Dithered\" shading mode.
// Only the alpha channel is used to indicate if the pixel should be lit or not.
uniform sampler2D dither_texture : filter_nearest;
uniform int dither_levels = 0;

// Noise specification for the shading.
uniform int shading_noise_cell_amount = 40;
uniform float shading_noise_strength : hint_range(-1.0, 1.0, 0.01) = 0.3;
uniform vec3 shading_noise_period = vec3(9.5, 9.5, 9.5);

// modulo function that returns positive wrapped values. (13 % 5 = 3, but -13 % 5 = 2)
vec3 modulo(vec3 divident, vec3 divisor){
	vec3 positiveDivident = mod(divident, divisor) + divisor;
	return mod(positiveDivident, divisor);
}

// 3 dimensional pseudo random number generator
vec3 random(vec3 value){
	vec3 return_value = vec3( dot(value, vec3(127.1,311.7, 201.9) ),
				  dot(value, vec3(269.5,183.3, 367.7) ),
				  dot(value, vec3(245.1,367.7, 105.6) ) );
	return -1.0 + 2.0 * fract(sin(return_value) * 43758.5453123);
}

// Calculate z-position of sphere surface
float calculate_z(vec2 uv, float radius) {
	// equation of a sphere at coordinates (0.5, 0.5)
	// (x-.5)^2 + (y-.5)^2 + z^2 = surface_radius^2
	float w = sqrt(pow(radius, 2.) - pow(uv.x - .5, 2.) - pow(uv.y - .5, 2.));
	return w;
}

mat3 rotate_x(float angle) {
    float s = sin(angle);
    float c = cos(angle);
    return mat3(
        vec3(1, 0, 0),
        vec3(0, c, -s),
        vec3(0, s, c)
    );
}

mat3 rotate_y(float angle) {
    float s = sin(angle);
    float c = cos(angle);
    return mat3(
        vec3(c, 0, s),
        vec3(0, 1, 0),
        vec3(-s, 0, c)
    );
}

// construct rotation matrix about 1/sqrt(2)i, 1/sqrt(2)j, 0k unit vector
// because UV coordinates start at the top left and rotating the noise
// rotates all the noise, not just the sphere so if you rotate about any
// axis of the coordinate system the noise moves through the sphere
// and it doesn't look like it's spinning (duh)
// it's also why it rotates on a tilted axis
// because this was the easiest solution I found
mat3 generate_rotation_matrix(float theta) {
	vec3 rot_1 = vec3(
		.5*(1.-cos(theta)) + cos(theta),
		.5*(1.-cos(theta)),
		-(1./sqrt(2))*sin(theta)
	);
	vec3 rot_2 = vec3(
		.5*(1.-cos(theta)),
		.5*(1.-cos(theta)) + cos(theta),
		(1./sqrt(2))*sin(theta)
	);
	vec3 rot_3 = vec3(
		(1./sqrt(2))*sin(theta),
		-(1./sqrt(2))*sin(theta),
		cos(theta)
	);
	return mat3(rot_1, rot_2, rot_3);
}

// Put the UV on a pixel grid.
vec2 calculate_grid_uv(vec2 uv) {
	return round(uv * pixel_scale) / pixel_scale;
}

// Return the colour from a palette that most closely matches target.
vec3 get_nearest_colour(in sampler2D palette, in vec3 target) {
	ivec2 palette_size = textureSize(palette, 0);
	vec3 closest_color = background_colour.rgb;
	float min_dist = 2.0;
	int rows = palette_size.y;
	int columns = palette_size.x;
	float n = float(columns);

	for (int row = 0; row < palette_size.y; row++) {
		float yuv = 1.000 / (2.000 * float(rows)) + float(row) / float(rows);
		for (int column = 0; column < columns; column++){
			float xuv = 1.000 / (2.000 * n) + float(column) / n;
			vec3 index_color = texture(palette, vec2(xuv,yuv)).rgb;
			float dist = length(index_color - target);
			if (dist < min_dist) {
				min_dist = dist;
				closest_color = index_color;
				// If the current colour is really really close, just return it
				// and don't bother checking the rest.
				if (min_dist < COLOUR_ESTIMATION_CUTOFF){
					return closest_color;
				}
			}
		}
	}
	return closest_color;
}

// Get the inverse alpha from a dither texture for the specified UV.
float get_leveled_dither(int level, vec2 uv) {
	ivec2 texture_size = textureSize(dither_texture, 0);
	int level_width = texture_size.x / dither_levels;

	float pattern_x = mod(uv.x * pixel_scale, float(level_width));
	// Add an offset for the pattern location
	pattern_x = pattern_x + float(level_width * level);
	float duvx = pattern_x / float(texture_size.x);

	float duvy = round(
		mod(uv.y * pixel_scale, float(texture_size.y))
	) / float(texture_size.y);

	vec2 dither_uv = vec2(
		duvx,
		duvy
	);
	return 1.0 - texture(dither_texture, dither_uv).a;
}

// Get a colour from a palette by row and column index.
vec4 get_colour(sampler2D palette, ivec2 palette_size, int column, int row) {
	float uvx = (1.0 / float(palette_size.x + 1)) * (float(column + 1));
	float uvy = (1.0 / float(palette_size.y + 1)) * (float(row + 1));
	return texture(palette, vec2(uvx, uvy));
}

// seamless noise function adapted from Godot Shaders seamless Perlin Noise function
float seamless_noise(vec2 uv, float w, vec3 _period, mat3 rot_p, int cell_amount) {
	vec3 uvw = rot_p * vec3(uv, w); // multiply by rotation vector to spin world
	uvw = vec3(uvw * float(cell_amount)); // multiply by cell amount and then round to create discrete cells
	vec3 cells_minimum = floor(uvw);
	vec3 cells_maximum = ceil(uvw);
	vec3 uvw_fract = fract(uvw);

	// wrap every period
	cells_minimum = modulo(cells_minimum, _period);
	cells_maximum = modulo(cells_maximum, _period);

	// calc lerp
	vec3 blur = smoothstep(0.3, 1.0, uvw_fract);

	// generate cube of pseudo-random values for every pixel
	vec3 p_000 = random(vec3(cells_minimum.x, cells_minimum.y, cells_minimum.z));
	vec3 p_100 = random(vec3(cells_maximum.x, cells_minimum.y, cells_minimum.z));
	vec3 p_010 = random(vec3(cells_minimum.x, cells_maximum.y, cells_minimum.z));
	vec3 p_110 = random(vec3(cells_maximum.x, cells_maximum.y, cells_minimum.z));
	vec3 p_001 = random(vec3(cells_minimum.x, cells_minimum.y, cells_maximum.z));
	vec3 p_101 = random(vec3(cells_maximum.x, cells_minimum.y, cells_maximum.z));
	vec3 p_011 = random(vec3(cells_minimum.x, cells_maximum.y, cells_maximum.z));
	vec3 p_111 = random(vec3(cells_maximum.x, cells_maximum.y, cells_maximum.z));

	// return a smoothed version of the noise
	return mix(mix( mix( dot( p_000, uvw_fract - vec3(0, 0, 0) ),
                     dot( p_100, uvw_fract - vec3(1, 0, 0) ), blur.x),
                mix( dot( p_010, uvw_fract - vec3(0, 1, 0) ),
                     dot( p_110, uvw_fract - vec3(1, 1, 0) ), blur.x), blur.y),
			mix( mix( dot( p_001, uvw_fract - vec3(0, 0, 1) ),
                     dot( p_101, uvw_fract - vec3(1, 0, 1) ), blur.x),
                mix( dot( p_011, uvw_fract - vec3(0, 1, 1) ),
                     dot( p_111, uvw_fract - vec3(1, 1, 1) ), blur.x), blur.y), blur.z) * 0.8 + 0.5;
}

// Calculate the light at the given radius.
float calculate_light(vec2 grid_uv, float radius) {

	// Some shading types do not need the light to be calculated, so return
	// early for those.
	if (shading_mode == SHADING_MODE_NONE) {
		return 1.0;
	} else if (shading_mode == SHADING_MODE_MAX) {
		return maximum_brightness;
	}

	// Normalize UV coordinates
    vec2 normalized_uv = (grid_uv - vec2(0.5)) / radius;

    // Convert UV to 3D point on sphere
    vec3 sphere_point = vec3(
        normalized_uv.x,
        sqrt(max(0.0, 1.0 - dot(normalized_uv, normalized_uv))),
        normalized_uv.y
    );

	float light = dot(
		normalize(
			sphere_point
		),
		normalize(sun_direction)
	);
	float brightness_cap = maximum_brightness / sun_strength;

	// Introduce some noise into the light to create more interesting shading,
	// if requested.
	if (shading_noise_strength != 0.0) {
		float w = calculate_z(grid_uv, radius);
		float noise = seamless_noise(
			grid_uv,
			w,
			shading_noise_period,
			mat3(1.0),
			shading_noise_cell_amount
		);
		if (!isnan(light)) {
			light = clamp (
				light + ((noise - 0.5) * shading_noise_strength),
				0.0,
				1.0
			);
		}
	}

	// Adjust the light value for the selected shading type.
	if (shading_mode == SHADING_MODE_SMOOTH) {
		light = sun_strength * min(
			brightness_cap,
			(light * shade_transition) / shade_step
		);
	} else if (shading_mode == SHADING_MODE_QUANT) {
		// Adding the floor results in a quantised gradient.
		light = sun_strength * min(
			brightness_cap,
			floor(light * shade_transition) / shade_step
		);
	} else if (shading_mode == SHADING_MODE_DITHER) {
		// When using dithering, the quantisation is done by dividing into
		// different types of dither below.
		light = sun_strength * min(
			brightness_cap,
			(light * shade_transition) / shade_step
		);

		ivec2 texture_size = textureSize(dither_texture, 0);
		for (int level = 0; level < dither_levels; level++) {
			float cutoff = float(level + 1) * (1.0 / float(dither_levels));
			if (light <= cutoff) {
				light = maximum_brightness * get_leveled_dither(
					level,
					grid_uv
				);
				break;
			}
		}
	}

	return light;
}

// Calculate additional noise and combine it with the existing noise for the
// surface.
float apply_surface_noise(
	float perlin,
	vec3 period,
	vec2 uv,
	float w,
	mat3 rot_p,
	int cell_amount,
	float strength
) {
	if (isnan(perlin)) {
		return perlin;
	}
	float result = perlin;
	float noise = seamless_noise(
		uv,
		w,
		period,
		rot_p,
		cell_amount
	);
	result = clamp(
		perlin + ((noise - 0.5) * strength),
		0.0,
		1.0
	);
	return result;
}

// Generate the surface.
vec4 surface(vec2 uv, mat3 rot_p, mat3 rot_p2,  mat3 rot_p3){
	float w = calculate_z(uv, surface_radius);
	float perlin = seamless_noise(
		uv,
		w,
		surface_primary_period,
		rot_p,
		surface_primary_cell_amount
	);
	if (!isnan(perlin)) {
		perlin = clamp (
			((perlin - 0.5) * surface_primary_strength) + 0.5,
			0.0,
			1.0
		);
	}
	if (!isnan(perlin)) {
		vec3 uvw = rot_p * vec3(uv, w);
		// The bump map will be mirrored on the other side of the sphere because
		// the UV values wrap around. This is convenient for certain effects
		// anyway, and if there is a way to wrap the texture around the whole
		// sphere I'm not sure how to do it.
		// The back side can be unmirrored (but still repeated) by flipping the
		// sign of the x component of the vector if the z component is < 0.0,
		// but you may get seams.
		/*if (uvw.z < 0.0) {
			uvw.x = -uvw.x;
		}*/
		vec4 bump = texture(surface_bump_map, uvw.xy);
		if (bump.a > 0.0) {
			perlin = clamp(
				perlin + ((bump.r - 0.5) * surface_bump_strength),
				0.0,
				1.0
			);
		}
	}
	if (surface_secondary_enabled) {
		perlin = apply_surface_noise(
			perlin,
			surface_secondary_period,
			uv,
			w,
			rot_p * rot_p2,
			surface_secondary_cell_amount,
			surface_secondary_strength
		);
	}
	if (surface_tertiary_enabled) {
		perlin = apply_surface_noise(
			perlin,
			surface_tertiary_period,
			uv,
			w,
			rot_p * rot_p3,
			surface_tertiary_cell_amount,
			surface_tertiary_strength
		);
	}

	// Default the colour to transparent outside the sphere, or the background
	// colour within it. This prevents shaded areas from being transparent.
	vec4 color = vec4(0);
	if (perlin <= 1.) {
		color = background_colour;
	}

	// Iterate through the palette and assign colours to the surface based
	// on the generated noise/bumpmap.
	ivec2 palette_size = textureSize(surface_palette, 0);
	float last_cutoff = -0.1;
	for (int column = 0; column <= palette_size.x; column++) {
		float cutoff;
		float animation_factor = 0.0;
		if (palette_size.y > 1) {
			vec4 spec = get_colour(surface_palette, palette_size, column, 1);
			// The red channel in the second row of the texture indicates the cutoff.
			cutoff = spec.r;
			// The last colour has to have a cutoff of 1.0 or the planet surface
			// will have holes in it.
			if (column == palette_size.x) {
				cutoff = 1.0;
			}

			// The green channel controls how much to animate each colour, in a
			// swelling/shrinking manner.
			animation_factor = spec.g;
			if (animation_factor > 0.0) {
				// Is 512 a good value here? Original code had 300 and 250 for
				// two of the colours, so I thought it was high enough to allow
				// that to be reproduced.
				cutoff = cutoff + sin(TIME)/(512.0 * (1.0 - animation_factor));
			}
		} else {
			// No explicit cutoffs provided - just split the colours equally.
			cutoff = (1.0 / float(palette_size.x)) * float(column + 1);
		}
		if (perlin > last_cutoff && perlin <= cutoff) {
			color = get_colour(surface_palette, palette_size, column, 0);
		}
		last_cutoff = cutoff;
	}

	float light = calculate_light(uv, surface_radius);
	color.rgb = color.rgb * light;
	return color;
}

// Generate clouds
vec4 clouds(
	float cover,
	vec2 uv,
	vec3 period,
	int cell_amount,
	vec3 noise_period,
	int noise_cell_amount,
	float noise_strength,
	mat3 rot_p,
	float radius,
	sampler2D palette,
	float fluctuation_speed,
	bool shadow
){

	if (cover == 0.0) {
		return vec4(0.0);
	}

	float w = calculate_z(uv, radius);
	float theta = TIME * fluctuation_speed;
	// rotate about 0,0 on UV coords and with the rot_p matrix (see below)
	mat3 rot_0 = mat3(
		vec3(1, 0, 0),
		vec3(0, cos(theta), -sin(theta)),
		vec3(0, sin(theta), cos(theta))
	);

	float perlin = seamless_noise(
		uv,
		w,
		period,
		rot_0*rot_p,
		cell_amount
	);
	float noise = seamless_noise(
		uv,
		w,
		noise_period,
		rot_0*rot_p,
		noise_cell_amount
	);
	if (!isnan(perlin)) {
		perlin = clamp (
			perlin + ((noise - 0.5) * noise_strength),
			0.0,
			1.0
		);
	}

	float base_cutoff = 1.0 - cover;
	vec4 color = vec4(0.);

	if (shadow) {
		if (perlin < base_cutoff){
			color = vec4(.00);
		}else if (perlin <= 1.0){
			color = vec4(0.0, 0.0, 0.0, cloud_shadow_darkness);
		};
	} else {
		ivec2 palette_size = textureSize(palette, 0);

		float last_cutoff = base_cutoff;
		for (int column = 0; column < palette_size.x; column++) {
			float cutoff;
			float animation_factor = 0.0;

			if (palette_size.y > 1) {
				vec4 spec = get_colour(palette, palette_size, column, 1);
				// The red channel in the second row of the texture indicates
				// the cutoff. In this case the range is between the base cutoff
				// and 1.
				cutoff = base_cutoff + ((1.0-base_cutoff) * spec.r);
				// The last colour has to have a cutoff of 1.0 or the clouds
				// will have weird holes in them.
				if (column == palette_size.x - 1) {
					cutoff = 1.0;
				}

				// The green channel controls how much to animate each colour,
				// in a swelling/shrinking manner.
				animation_factor = spec.g;
				if (animation_factor > 0.0) {
					// Is 512 a good value here? Original code had 300 and 250
					// for two of the colours, so I thought it was high enough
					// to allow that to be reproduced.
					cutoff = cutoff + sin(TIME)/(512.0 * (1.0 - animation_factor));
				}
			} else {
				// No explicit cutoffs provided - just split the colours equally.
				// In this case the range is between the base cutoff and 1.
				cutoff = base_cutoff + (
					(1.0 - base_cutoff) / float(palette_size.x)
				) * float(column + 1);
			}

			if (perlin < base_cutoff){
				color = vec4(0.0);
			}else if (perlin > last_cutoff && perlin <= cutoff){
				color = get_colour(palette, palette_size, column, 0);
			};
			last_cutoff = cutoff;
		}
	}

	float light = calculate_light(uv, radius);
	color.rgb = color.rgb * light;
	return color;
}

vec4 atmosphere(vec2 uv) {
	vec4 result = vec4(0.0);

	if (atmosphere_radius <= surface_radius) {
		return result;
	}

	ivec2 palette_size = textureSize(atmosphere_palette, 0);
	float atmosphere_thickness = (atmosphere_radius - surface_radius);
	float atmos_layer_thickness = atmosphere_thickness / float(palette_size.x);

	for (int column = palette_size.x - 1; column >= 0; column--) {
		float radius = surface_radius + (
			atmos_layer_thickness * float(column + 1)
		);
		float w = calculate_z(uv, radius);

		if (w < 1.0) {
			float light = calculate_light(uv, radius);
			result = get_colour(atmosphere_palette, palette_size, column, 0);
			result.rgb = result.rgb * light;
		}
	}
	return result;
}

void fragment() {
	// Put the UV on a pixel grid.
	vec2 grid_uv = calculate_grid_uv(UV);

	// Surface rotation matrices
	mat3 srot = generate_rotation_matrix(
		surface_base_rotation + (TIME * surface_rotation_speed)
	);
	mat3 srot2 = generate_rotation_matrix(
		surface_secondary_rotation + (TIME * surface_secondary_rotation_speed)
	);
	mat3 srot3 = generate_rotation_matrix(
		surface_tertiary_rotation + (TIME * surface_tertiary_rotation_speed)
	);

	vec4 color = surface(grid_uv, srot, srot2, srot3);
	vec4 clouds_1_shadows = vec4(0.0);
	vec4 clouds_1 = vec4(0.0);
	if (cloud_1_cover > 0.0) {
		// Cloud rotation matrix
		mat3 crot = generate_rotation_matrix(
			TIME * cloud_1_rotation_speed
		);
		if (display_cloud_shadows) {
			// Get shadow colors
			clouds_1_shadows = clouds(
				cloud_1_cover,
				grid_uv,
				cloud_1_period,
				cloud_1_cell_amount,
				cloud_1_secondary_period,
				cloud_1_secondary_cell_amount,
				cloud_1_secondary_strength,
				crot,
				surface_radius,
				cloud_1_palette,
				cloud_1_fluctuation_speed,
				true
			);
		}
		// Get cloud colors
		clouds_1 = clouds(
			cloud_1_cover,
			grid_uv,
			cloud_1_period,
			cloud_1_cell_amount,
			cloud_1_secondary_period,
			cloud_1_secondary_cell_amount,
			cloud_1_secondary_strength,
			crot,
			cloud_1_radius,
			cloud_1_palette,
			cloud_1_fluctuation_speed,
			false
		);
	}

	vec4 clouds_2_shadows = vec4(0.0);
	vec4 clouds_2 = vec4(0.0);
	if (cloud_2_cover > 0.0) {
		// Cloud rotation matrix
		mat3 crot = generate_rotation_matrix(
			TIME * cloud_2_rotation_speed
		);
		if (display_cloud_shadows) {
			// Get shadow colors
			clouds_2_shadows = clouds(
				cloud_2_cover,
				grid_uv,
				cloud_2_period,
				cloud_2_cell_amount,
				cloud_2_secondary_period,
				cloud_2_secondary_cell_amount,
				cloud_2_secondary_strength,
				crot,
				surface_radius,
				cloud_2_palette,
				cloud_2_fluctuation_speed,
				true
			);
		}
		// Get cloud colors
		clouds_2 = clouds(
			cloud_2_cover,
			grid_uv,
			cloud_2_period,
			cloud_2_cell_amount,
			cloud_2_secondary_period,
			cloud_2_secondary_cell_amount,
			cloud_2_secondary_strength,
			crot,
			cloud_2_radius,
			cloud_2_palette,
			cloud_2_fluctuation_speed,
			false
		);
	}

	COLOR = vec4(0., 0., 0., 0.);

	COLOR.rgba = atmosphere(grid_uv);
	if (color.a > 0.0) {
		COLOR.rgba = color.rgba; // draw the world
		// Darken for the cloud shadows.
		COLOR.rgb = COLOR.rgb * (1.0 - clouds_1_shadows.a);
		COLOR.rgb = COLOR.rgb * (1.0 - clouds_2_shadows.a);
	}

	// Add the clouds
	// This requires fully opaque clouds because the partially
	// transparent clouds were not behaving well with the lighting.
	if (clouds_1.a >= 1.0) {
		COLOR.rgba = clouds_1.rgba;
	}
	if (clouds_2.a >= 1.0) {
		COLOR.rgba = clouds_2.rgba;
	}

	// Posterise
	if (posterise) {
		COLOR.rgb = get_nearest_colour(posterisation_palette, COLOR.rgb);
	}
}"

[sub_resource type="Gradient" id="Gradient_mwfav"]
offsets = PackedFloat32Array(0.036764707, 0.5735294, 0.8897059)
colors = PackedColorArray(0.2231029, 0.2231029, 0.22310287, 1, 0.61540955, 0.5207453, 0.5161481, 1, 0.8089983, 0.8089983, 0.8089982, 1)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_3p2gp"]
gradient = SubResource("Gradient_mwfav")

[sub_resource type="ShaderMaterial" id="ShaderMaterial_cvmbd"]
shader = SubResource("Shader_qmy6f")
shader_parameter/pixel_scale = 389.0
shader_parameter/surface_radius = 0.29000001307648005
shader_parameter/surface_palette = SubResource("GradientTexture1D_3p2gp")
shader_parameter/background_colour = Color(0, 0, 0, 1)
shader_parameter/surface_base_rotation = 0.0
shader_parameter/surface_rotation_speed = 0.3540000643150001
shader_parameter/surface_primary_cell_amount = 17
shader_parameter/surface_primary_strength = 1.0
shader_parameter/surface_primary_period = Vector3(9.5, 9.5, 9.5)
shader_parameter/surface_secondary_enabled = true
shader_parameter/surface_secondary_cell_amount = 20
shader_parameter/surface_secondary_strength = 0.3
shader_parameter/surface_secondary_period = Vector3(9.5, 9.5, 9.5)
shader_parameter/surface_secondary_rotation = 0.0
shader_parameter/surface_secondary_rotation_speed = 0.0
shader_parameter/surface_tertiary_enabled = false
shader_parameter/surface_tertiary_cell_amount = 30
shader_parameter/surface_tertiary_strength = 0.3
shader_parameter/surface_tertiary_period = Vector3(9.5, 9.5, 9.5)
shader_parameter/surface_tertiary_rotation = 0.0
shader_parameter/surface_tertiary_rotation_speed = 0.0
shader_parameter/surface_bump_strength = 0.999999955296
shader_parameter/display_cloud_shadows = false
shader_parameter/cloud_shadow_darkness = 0.25
shader_parameter/cloud_1_cover = 0.3
shader_parameter/cloud_1_radius = 0.32
shader_parameter/cloud_1_cell_amount = 10
shader_parameter/cloud_1_rotation_speed = 0.11300005286749992
shader_parameter/cloud_1_fluctuation_speed = 0.05500005011250009
shader_parameter/cloud_1_period = Vector3(9.5, 9.5, 9.5)
shader_parameter/cloud_1_secondary_period = Vector3(9.5, 9.5, 9.5)
shader_parameter/cloud_1_secondary_cell_amount = 10
shader_parameter/cloud_1_secondary_strength = 0.3
shader_parameter/cloud_2_cover = 0.0
shader_parameter/cloud_2_radius = 0.32
shader_parameter/cloud_2_cell_amount = 10
shader_parameter/cloud_2_rotation_speed = 0.03
shader_parameter/cloud_2_fluctuation_speed = 0.03
shader_parameter/cloud_2_period = Vector3(9.5, 9.5, 9.5)
shader_parameter/cloud_2_secondary_period = Vector3(9.5, 9.5, 9.5)
shader_parameter/cloud_2_secondary_cell_amount = 10
shader_parameter/cloud_2_secondary_strength = 0.3
shader_parameter/atmosphere_radius = 0.34
shader_parameter/posterise = false
shader_parameter/shading_mode = 1
shader_parameter/maximum_brightness = 1.10000001639132
shader_parameter/sun_strength = 3.0
shader_parameter/shade_transition = 10.0
shader_parameter/shade_step = 8.0
shader_parameter/sun_direction = Vector3(1, 0.2, -0.3)
shader_parameter/dither_levels = 0
shader_parameter/shading_noise_cell_amount = 40
shader_parameter/shading_noise_strength = 0.3
shader_parameter/shading_noise_period = Vector3(9.5, 9.5, 9.5)

[sub_resource type="AnimatedTexture" id="AnimatedTexture_lv1af"]
frames = 4
frame_0/texture = ExtResource("17_1iba3")
frame_0/duration = 0.1
frame_1/texture = ExtResource("18_yxlcp")
frame_1/duration = 0.1
frame_2/texture = ExtResource("19_ebg2g")
frame_2/duration = 0.1
frame_3/texture = ExtResource("20_2ofn6")
frame_3/duration = 0.1

[node name="Main" type="Node" unique_id=319315482]

[node name="Background" type="ColorRect" parent="." unique_id=1502370919]
anchors_preset = -1
anchor_left = -2.6030002
anchor_top = -3.3890002
anchor_right = 3.6430001
anchor_bottom = 4.559
offset_left = -0.26391602
offset_top = 0.23217773
offset_right = 0.26464844
offset_bottom = 0.2319336
grow_horizontal = 2
grow_vertical = 2
color = Color(0, 0, 0, 1)

[node name="Stars" type="GPUParticles2D" parent="Background" unique_id=1245376643]
position = Vector2(3569.9202, 2521.8398)
amount = 900
texture = ExtResource("1_q8u5g")
lifetime = 10.0
explosiveness = 0.5
trail_lifetime = 10.0
process_material = SubResource("ParticleProcessMaterial_e5dxq")

[node name="BlackHole" parent="." unique_id=1154158881 instance=ExtResource("2_0bbpv")]

[node name="Player" parent="." unique_id=427548273 instance=ExtResource("4_rarhs")]
position = Vector2(578, 327)

[node name="Notes" parent="." unique_id=479660983 instance=ExtResource("4_vcsgt")]

[node name="Camera2D" type="Camera2D" parent="." unique_id=1854921618]
position = Vector2(576, 335)
zoom = Vector2(0.5, 0.5)
position_smoothing_enabled = true
script = ExtResource("5_vcsgt")

[node name="UI" type="CanvasLayer" parent="." unique_id=1991981816]
script = ExtResource("6_nxtc6")

[node name="critical_mass_recovery" type="Timer" parent="UI" unique_id=1926564720]
wait_time = 5.0
autostart = true

[node name="menu_music" type="AudioStreamPlayer" parent="UI" unique_id=643854300]

[node name="Label" type="Label" parent="UI" unique_id=1044558571]
anchors_preset = 1
anchor_left = 1.0
anchor_right = 1.0
offset_left = -40.0
offset_bottom = 23.0
grow_horizontal = 0
text = "Press Esc to pause and P to leave"

[node name="combo" type="RichTextLabel" parent="UI" unique_id=800462834]
modulate = Color(1, 1, 1, 0)
anchors_preset = -1
anchor_left = 0.44500002
anchor_top = 0.1
anchor_right = 0.555
anchor_bottom = 0.1
offset_left = -53.640015
offset_top = -33.800003
offset_right = 61.640015
offset_bottom = 43.399994
grow_horizontal = 2
grow_vertical = 2
pivot_offset_ratio = Vector2(0.5, 0.5)
theme_override_fonts/normal_font = ExtResource("7_nxtc6")
theme_override_font_sizes/normal_font_size = 32
bbcode_enabled = true
text = "COMBO  x9"
fit_content = true
horizontal_alignment = 1
vertical_alignment = 1

[node name="recap_screen" type="RichTextLabel" parent="UI" unique_id=1747555886]
visible = false
anchors_preset = -1
anchor_left = 0.44500002
anchor_top = 0.1
anchor_right = 0.555
anchor_bottom = 0.1
offset_left = -110.640015
offset_top = -33.800003
offset_right = 118.640015
offset_bottom = 43.399994
grow_horizontal = 2
grow_vertical = 2
pivot_offset_ratio = Vector2(0.5, 0.5)
theme_override_fonts/normal_font = ExtResource("7_nxtc6")
theme_override_font_sizes/normal_font_size = 32
bbcode_enabled = true
text = "end_stuff
"
fit_content = true
horizontal_alignment = 1
vertical_alignment = 1

[node name="Menu" type="Control" parent="UI" unique_id=1210793270]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2

[node name="Background" type="ColorRect" parent="UI/Menu" unique_id=1679548209]
layout_mode = 1
anchors_preset = -1
anchor_left = -2.6030002
anchor_top = -3.3890002
anchor_right = 3.6430001
anchor_bottom = 4.559
offset_left = 39.65625
offset_top = 6.0720215
offset_right = 40.18457
offset_bottom = 5.60791
grow_horizontal = 2
grow_vertical = 2
mouse_filter = 2
color = Color(0, 0, 0, 1)

[node name="Stars?" type="GPUParticles2D" parent="UI/Menu/Background" unique_id=880914782]
position = Vector2(3569.9202, 2521.8398)
amount = 300
texture = ExtResource("1_q8u5g")
lifetime = 100.0
explosiveness = 1.0
trail_lifetime = 10.0
process_material = SubResource("ParticleProcessMaterial_e5dxq")

[node name="first planet" type="ColorRect" parent="UI/Menu" unique_id=134270359]
material = SubResource("ShaderMaterial_bjd11")
layout_mode = 1
anchors_preset = 1
anchor_left = 1.0
anchor_right = 1.0
offset_left = -554.0
offset_top = -57.0
offset_right = 76.0
offset_bottom = 469.0
grow_horizontal = 0
mouse_filter = 2

[node name="second planet" type="ColorRect" parent="UI/Menu" unique_id=477740230]
material = SubResource("ShaderMaterial_cvmbd")
layout_mode = 1
anchors_preset = -1
anchor_top = 1.0
anchor_bottom = 1.0
offset_left = -116.0
offset_top = -330.0
offset_right = 288.0
offset_bottom = -4.0
grow_horizontal = 2
grow_vertical = 2
mouse_filter = 2

[node name="Button" type="Button" parent="UI/Menu" unique_id=248484021]
layout_mode = 0
offset_right = 115.0
offset_bottom = 31.0
text = "Back to menu"
flat = true

[node name="Purple\'s advenure" type="Control" parent="UI/Menu" unique_id=179894255]
unique_name_in_owner = true
layout_mode = 1
anchor_left = 0.3
anchor_top = 0.1
anchor_right = 0.70000005
anchor_bottom = 0.591
offset_left = -3.0517578e-05
offset_right = 86.599976
offset_bottom = -58.16803
grow_horizontal = 2
grow_vertical = 0

[node name="right-arrow" type="TextureButton" parent="UI/Menu/Purple\'s advenure" unique_id=1166018054]
custom_minimum_size = Vector2(40, 40)
layout_mode = 1
anchors_preset = -1
anchor_left = 0.5583021
anchor_top = 0.039
anchor_right = 0.63244146
anchor_bottom = 0.193
texture_normal = ExtResource("9_e2s0e")
texture_hover = ExtResource("10_gsxep")
stretch_mode = 5
flip_h = true
metadata/_edit_use_anchors_ = true

[node name="left-arrow" type="TextureButton" parent="UI/Menu/Purple\'s advenure" unique_id=1871543125]
custom_minimum_size = Vector2(40, 40)
layout_mode = 1
anchors_preset = -1
anchor_left = 0.21303359
anchor_top = 0.03923077
anchor_right = 0.27255836
anchor_bottom = 0.19284616
texture_normal = ExtResource("9_e2s0e")
texture_hover = ExtResource("10_gsxep")
stretch_mode = 5
metadata/_edit_use_anchors_ = true

[node name="Title" type="Label" parent="UI/Menu/Purple\'s advenure" unique_id=1972863213]
layout_mode = 1
anchors_preset = -1
anchor_left = 0.41900003
anchor_top = 0.07
anchor_right = 0.41900003
anchor_bottom = 0.07
offset_left = -73.4606
offset_right = 73.5394
offset_bottom = 22.999996
grow_horizontal = 2
text = "Purple's Adventure"

[node name="Tutorial" parent="UI/Menu/Purple\'s advenure" unique_id=369980723 instance=ExtResource("12_fs0b5")]
layout_mode = 1
anchor_left = -0.088783294
anchor_top = 0.2853846
anchor_right = 0.14521672
anchor_bottom = 0.7773846
offset_left = 0.0
offset_top = 0.0
offset_right = -0.09161377
offset_bottom = 0.0799942
left = true

[node name="LevelBase" parent="UI/Menu/Purple\'s advenure" unique_id=1710373079 instance=ExtResource("12_fs0b5")]
layout_mode = 1
anchor_left = 0.40073726
anchor_top = 0.28130773
anchor_right = 0.63441324
anchor_bottom = 0.78923076
offset_right = 0.122161865
offset_bottom = 0.0
texture_normal = ExtResource("13_hq6as")
texture_hover = ExtResource("14_kxyth")
song = ExtResource("13_ma7h6")

[node name="LevelBase2" parent="UI/Menu/Purple\'s advenure" unique_id=1665444683 instance=ExtResource("12_fs0b5")]
layout_mode = 1
anchor_left = 0.06825651
anchor_top = 1.2505385
anchor_right = 0.3019325
anchor_bottom = 1.7584616
offset_right = 0.122161865
offset_bottom = 0.0
texture_normal = ExtResource("16_cobve")
texture_hover = ExtResource("17_cmqll")
left = true
song = ExtResource("14_gsxep")

[node name="LevelBase3" parent="UI/Menu/Purple\'s advenure" unique_id=1894125025 instance=ExtResource("12_fs0b5")]
layout_mode = 1
anchor_left = 0.53774863
anchor_top = 1.262077
anchor_right = 0.77142465
anchor_bottom = 1.7700001
offset_right = 0.122161865
offset_bottom = 0.0
texture_normal = ExtResource("19_8cp6k")
texture_hover = ExtResource("20_8b871")
song = ExtResource("23_8b871")

[node name="Neru\'s kingdom" type="Control" parent="UI/Menu" unique_id=1845890837]
unique_name_in_owner = true
visible = false
layout_mode = 1
anchor_left = 0.3
anchor_top = 0.1
anchor_right = 0.70000005
anchor_bottom = 0.591
offset_left = -3.0517578e-05
offset_right = 86.599976
offset_bottom = -58.16803
grow_horizontal = 2
grow_vertical = 0

[node name="right-arrow" type="TextureButton" parent="UI/Menu/Neru\'s kingdom" unique_id=810322967]
custom_minimum_size = Vector2(40, 40)
layout_mode = 1
anchors_preset = -1
anchor_left = 0.5583021
anchor_top = 0.039
anchor_right = 0.63244146
anchor_bottom = 0.193
offset_left = 3.0517578e-05
offset_right = 3.0517578e-05
texture_normal = ExtResource("9_e2s0e")
texture_hover = ExtResource("10_gsxep")
stretch_mode = 5
flip_h = true
metadata/_edit_use_anchors_ = true

[node name="left-arrow" type="TextureButton" parent="UI/Menu/Neru\'s kingdom" unique_id=1772451809]
custom_minimum_size = Vector2(40, 40)
layout_mode = 1
anchors_preset = -1
anchor_left = 0.21303359
anchor_top = 0.03923077
anchor_right = 0.27255836
anchor_bottom = 0.19284616
offset_bottom = 0.059993744
texture_normal = ExtResource("9_e2s0e")
texture_hover = ExtResource("10_gsxep")
stretch_mode = 5
metadata/_edit_use_anchors_ = true

[node name="Title" type="Label" parent="UI/Menu/Neru\'s kingdom" unique_id=962519669]
layout_mode = 1
anchors_preset = -1
anchor_left = 0.41900003
anchor_top = 0.07
anchor_right = 0.41900003
anchor_bottom = 0.07
offset_left = -73.4606
offset_right = 73.5394
offset_bottom = 22.999996
grow_horizontal = 2
text = "Neru's Kingdom"

[node name="LevelBase4" parent="UI/Menu/Neru\'s kingdom" unique_id=1560834955 instance=ExtResource("12_fs0b5")]
unique_name_in_owner = false
layout_mode = 1
anchor_left = -0.088783294
anchor_top = 0.2853846
anchor_right = 0.14521672
anchor_bottom = 0.7773846
offset_left = 0.0
offset_top = 0.0
offset_right = -0.09161377
offset_bottom = 0.0799942
texture_normal = ExtResource("21_8b871")
texture_hover = ExtResource("22_pdwbg")
left = true
song = ExtResource("21_cmqll")

[node name="LevelBase" parent="UI/Menu/Neru\'s kingdom" unique_id=184686479 instance=ExtResource("12_fs0b5")]
unique_name_in_owner = false
layout_mode = 1
anchor_left = 0.40073726
anchor_top = 0.28130773
anchor_right = 0.63441324
anchor_bottom = 0.78923076
offset_right = 0.122161865
offset_bottom = 0.0
texture_normal = ExtResource("27_t70y2")
texture_hover = ExtResource("28_8gny2")
song = ExtResource("15_v0vrg")

[node name="LevelBase2" parent="UI/Menu/Neru\'s kingdom" unique_id=1853393123 instance=ExtResource("12_fs0b5")]
unique_name_in_owner = false
layout_mode = 1
anchor_left = 0.06825651
anchor_top = 1.2505385
anchor_right = 0.3019325
anchor_bottom = 1.7584616
offset_right = 0.122161865
offset_bottom = 0.0
texture_normal = ExtResource("24_5grtr")
texture_hover = ExtResource("25_vua0o")
left = true
song = ExtResource("22_8cp6k")

[node name="LevelBase3" parent="UI/Menu/Neru\'s kingdom" unique_id=891644021 instance=ExtResource("12_fs0b5")]
unique_name_in_owner = false
layout_mode = 1
anchor_left = 0.53774863
anchor_top = 1.262077
anchor_right = 0.77142465
anchor_bottom = 1.7700001
offset_right = 0.122161865
offset_bottom = 0.0
texture_normal = ExtResource("30_s3wmy")
texture_hover = ExtResource("31_gdcyb")
song = ExtResource("24_pdwbg")

[node name="Main_Menu" parent="UI/Menu" unique_id=2049669540 instance=ExtResource("25_pnhog")]
unique_name_in_owner = true
layout_mode = 1

[node name="MusicControl" type="Node2D" parent="." unique_id=359360928]
unique_name_in_owner = true
script = ExtResource("8_jlsqs")

[node name="Music" type="AudioStreamPlayer" parent="MusicControl" unique_id=748155325]

[node name="cursor" type="Sprite2D" parent="." unique_id=1541986173]
position = Vector2(-1944, -1570)
scale = Vector2(4, 4)
texture = SubResource("AnimatedTexture_lv1af")
script = ExtResource("21_xa025")

[node name="DustFogs" parent="." unique_id=2128655692 instance=ExtResource("22_yxlcp")]
position = Vector2(573, 362)

[node name="SaveStuff" parent="." unique_id=155425733 instance=ExtResource("42_pdwbg")]

[connection signal="timeout" from="UI/critical_mass_recovery" to="UI" method="_on_critical_mass_recovery_timeout"]
[connection signal="pressed" from="UI/Menu/Button" to="UI" method="_on_button_pressed"]
[connection signal="pressed" from="UI/Menu/Purple\'s advenure/right-arrow" to="UI" method="_on_rightarrow_pressed"]
[connection signal="pressed" from="UI/Menu/Purple\'s advenure/left-arrow" to="UI" method="_on_leftarrow_pressed"]
[connection signal="pressed" from="UI/Menu/Purple\'s advenure/Tutorial" to="UI" method="_on_tutorial_pressed"]
[connection signal="pressed" from="UI/Menu/Neru\'s kingdom/right-arrow" to="UI" method="_on_rightarrow_pressed"]
[connection signal="pressed" from="UI/Menu/Neru\'s kingdom/left-arrow" to="UI" method="_on_leftarrow_pressed"]
